'use strict';

angular.module('ben33App')
  .factory('createService', [function () {

  var concatEvent = function (desc) {
    var str = '';
    if (desc.eventName) {
      str = '#' + desc.eventName + '#';
    }
    if (desc.startDate) {
    }

  };

  }])
;
